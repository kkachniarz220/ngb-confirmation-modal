import { NgbModalConfirmationModalHeaderDirective } from './ngb-modal-confirmation-modal-header.directive';

describe('NgbModalConfirmationModalHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbModalConfirmationModalHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
