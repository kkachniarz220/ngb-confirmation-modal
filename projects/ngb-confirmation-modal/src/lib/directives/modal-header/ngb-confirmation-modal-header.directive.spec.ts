import { NgbConfirmationModalHeaderDirective } from './ngb-confirmation-modal-header.directive';

describe('NgbModalConfirmationModalHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbConfirmationModalHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
