import { NgbConfirmationModalBodyDirective } from './ngb-confirmation-modal-body.directive';

describe('NgbConfirmationModalBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbConfirmationModalBodyDirective();
    expect(directive).toBeTruthy();
  });
});
